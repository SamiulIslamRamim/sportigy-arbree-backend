enum UserRole {
  player
  organization
}

enum MatchResult {
  WIN
  LOSS
  DRAW
  TIE
  NO_RESULT
}

enum FieldSection {
  PROFILE
  MATCH
}

enum FieldType {
  SELECT
  MULTI_SELECT
  NUMBER
  TEXT
  BOOLEAN
  DATE
}

enum Gender {
  male
  female
  other
}

enum ApprovalStatus {
  PENDING
  APPROVED
  REJECTED
}

enum PlayerSide {
  HOME
  AWAY
}

enum FormulaRole {
  NUMERATOR
  DENOMINATOR
}
